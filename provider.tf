provider "aws" {
  region = "us-east-2"
  #My aws access and secret keys is in ~/.aws/credentials file in my local machine. However you can uncomment below and add it here if you would like

  /*
  access_key = "my-access-key"
  secret_key = "my-secret-key"
  */
}